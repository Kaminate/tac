
// This file would be the output of a shader graph

// -------------------------


// CornellBox-Original.obj vtx buffer only contains positions
// CornellBox-Original.mtl contains colors

SHADER_GRAPH_CBUF_SOURCE: WORLD_MATRIX
SHADER_GRAPH_CBUF_SOURCE: VTX_BUF_IDX
SHADER_GRAPH_CBUF_SOURCE: INPUT_LAYOUT_IDX

void GenerateMtlShd()
{


}
 
struct VS_OUTPUT
{
  float4 mPos_NDC : SV_POSITION;
};

VS_OUTPUT VSMain( uint iVtx : SV_VertexID )
{
  const matrix world = sCBuf.mWorld;
  const uint vtxBufIdx = sCBuf.mVtxBufIdx;

  const InputLayout inputLayout = sVtxBufs[ sCBuf.mInputLayoutIdx ].Load< InputLayout >( 0 );

  const uint vtxByteOffset = sVtxBufs[ sCBuf.mVtxBufIdx ].Load<  * ;

// -- BEGIN MTL SHD COMPILE -----------------------------------

  VS_OUTPUT GetVtxOutput()
  
  
// -- END MTL SHD COMPILE -----------------------------------


}




