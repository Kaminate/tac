# Src
```
    common/             Common files shared between all targets
    space/              Game dll, contains the world, inhabited by ghosts
    shell/              Core engine exe, platform specific code
```

